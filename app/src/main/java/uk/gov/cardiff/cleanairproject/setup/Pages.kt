package uk.gov.cardiff.cleanairproject.setup

/**
 * These enums are used to refer to fragments without the risk of causing errors like with strings.
 */
enum class Pages {
    WELCOME, LOCATION, BLUETOOTH, FINISHED, LOGIN
}